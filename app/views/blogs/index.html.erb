<% @posts.each_with_index do |post, index| %>
  <% if index >= 5 %>
  <% break %>
  <% else %>
  <h1><%= post.title %></h1>
  <p><%= post.updated_at %></p>
  <p><%= post.body.truncate(50) %></p>
  <%= link_to '続きを見る', blog_path(post.id) %>
  <% end %>
  <% index += 1 %>
<% end %>
